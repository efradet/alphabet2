export function levenshtein(a:string,b:string){const m=a.length,n=b.length;const d=Array.from({length:m+1},()=>new Array(n+1).fill(0));for(let i=0;i<=m;i++)d[i][0]=i;for(let j=0;j<=n;j++)d[0][j]=j;for(let i=1;i<=m;i++){for(let j=1;j<=n;j++){const c=a[i-1]===b[j-1]?0:1;d[i][j]=Math.min(d[i-1][j]+1,d[i][j-1]+1,d[i-1][j-1]+c)}}return d[m][n]}
export function similarity(a:string,b:string){if(!a&&!b)return 1;if(!a||!b)return 0;const dist=levenshtein(a,b);const denom=Math.max(a.length,b.length)||1;return 1-dist/denom}